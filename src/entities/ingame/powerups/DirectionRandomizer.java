package entities.ingame.powerups;

import entities.Entity;

public class DirectionRandomizer extends Entity {
    /*
    whenever the ball intersects its position it gives the ball a random new direction,
    then randomizer block disappears.
     */
}
